<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Document</title>
</head>
<body>
  <p><a href="/logout">logout</a></p>
  <h1><a href="/tables">all tables</a></h1>
  <h2><a href="/tables/<%= table.name %>"><%= table.name %></a></h1>
  <h3><a href="/tables/<%= table.name %>">Back</a></h3>
  <hr>
  <% items.forEach(function(item) { %>
    <% for (var key in item) { %>
      <p><%= key %>: <%= item[key] %></p>
    <% } %>
    <a href="/tables/<%= table.name %>/<%= item.id %>/edit"><button>Edit</button></a>
    <form action="/tables/<%= table.name %>/<%= item.id %>" method="post">
      <input type="hidden" name="_method" value="delete">
      <button>Delete</button>
    </form>
    <hr>
  <% }) %>
</body>
</html>